常用命令
readelf -d /path/to/executable | grep NEEDED
patchelf --replace-needed /path/to/lib.so lib.so executable
LD_LIBRARY_PATH=./libs ldd executable
sudo find / -name "lib.so*"
ldconfig -p | grep lib.so

常用方法
weston脚本加上ldd看库链接情况
WAYLAND_DISPLAY= XDG_DATA_HOME=$CONFDIR ldd $GAMEDIR/$game_executable 

截图
ffmpeg -f fbdev -i /dev/fb0 -vframes 1 screenshot.png